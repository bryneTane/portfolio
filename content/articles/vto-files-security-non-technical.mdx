---
title: "What happens to your photo when you upload it to MirrAI"
description: "A tour of the mechanisms keeping your photos private: EXIF stripping, RLS, signed URLs, etc..."
date: "2026-06-01"
tags: ["security", "explainer"]
---

This is how [MirrAI](https://mirraitryon.com) keeps your photos private between upload and result. The file picks up four protections along the way, each described below.

## Your photo carries hidden data: EXIF

Every photo your phone takes ships with a hidden block of metadata called **EXIF** (Exchangeable Image File Format). GPS coordinates, phone make and model, timestamp, lens. Cameras enable this by default.

Before your photo lands on disk, the file is rewritten inside an **Edge Function** (a small server next to the storage). Pixels stay. EXIF is dropped.

<div
  style={{
    display: "grid",
    gridTemplateColumns: "repeat(auto-fit, minmax(15rem, 1fr))",
    gap: "1rem",
    margin: "1.5rem 0",
  }}
>
  <div
    style={{
      background: "var(--fa-bad-bg)",
      border: "1.5px solid var(--fa-bad-border)",
      borderRadius: "12px",
      padding: "1.25rem",
      color: "var(--fa-text)",
    }}
  >
    <div
      style={{
        color: "var(--fa-bad-text)",
        fontWeight: 700,
        fontSize: "0.75rem",
        textTransform: "uppercase",
        letterSpacing: "0.08em",
        marginBottom: "0.85rem",
      }}
    >
      The EXIF your phone embeds
    </div>
    <div
      style={{
        display: "flex",
        flexDirection: "column",
        gap: "0.55rem",
        fontSize: "0.9rem",
        color: "var(--fa-text)",
      }}
    >
      <div
        style={{
          display: "flex",
          alignItems: "center",
          gap: "0.6rem",
          color: "var(--fa-text)",
        }}
      >
        <svg
          width="18"
          height="18"
          viewBox="0 0 24 24"
          fill="none"
          style={{ stroke: "var(--fa-bad-text)" }}
          strokeWidth="1.75"
          strokeLinecap="round"
          strokeLinejoin="round"
        >
          <path d="M20 10c0 6-8 12-8 12s-8-6-8-12a8 8 0 0 1 16 0z" />
          <circle cx="12" cy="10" r="3" />
        </svg>
        <span style={{ color: "var(--fa-text)" }}>
          GPS latitude, longitude, altitude
        </span>
      </div>
      <div
        style={{
          display: "flex",
          alignItems: "center",
          gap: "0.6rem",
          color: "var(--fa-text)",
        }}
      >
        <svg
          width="18"
          height="18"
          viewBox="0 0 24 24"
          fill="none"
          style={{ stroke: "var(--fa-bad-text)" }}
          strokeWidth="1.75"
          strokeLinecap="round"
          strokeLinejoin="round"
        >
          <rect x="5" y="2" width="14" height="20" rx="2" />
          <line x1="12" y1="18" x2="12.01" y2="18" />
        </svg>
        <span style={{ color: "var(--fa-text)" }}>Phone make and model</span>
      </div>
      <div
        style={{
          display: "flex",
          alignItems: "center",
          gap: "0.6rem",
          color: "var(--fa-text)",
        }}
      >
        <svg
          width="18"
          height="18"
          viewBox="0 0 24 24"
          fill="none"
          style={{ stroke: "var(--fa-bad-text)" }}
          strokeWidth="1.75"
          strokeLinecap="round"
          strokeLinejoin="round"
        >
          <circle cx="12" cy="12" r="10" />
          <polyline points="12 6 12 12 16 14" />
        </svg>
        <span style={{ color: "var(--fa-text)" }}>Exact date and time</span>
      </div>
      <div
        style={{
          display: "flex",
          alignItems: "center",
          gap: "0.6rem",
          color: "var(--fa-text)",
        }}
      >
        <svg
          width="18"
          height="18"
          viewBox="0 0 24 24"
          fill="none"
          style={{ stroke: "var(--fa-bad-text)" }}
          strokeWidth="1.75"
          strokeLinecap="round"
          strokeLinejoin="round"
        >
          <circle cx="12" cy="12" r="10" />
          <circle cx="12" cy="12" r="3" />
        </svg>
        <span style={{ color: "var(--fa-text)" }}>Lens, ISO, exposure</span>
      </div>
      <div
        style={{
          display: "flex",
          alignItems: "center",
          gap: "0.6rem",
          color: "var(--fa-dim)",
        }}
      >
        <svg
          width="18"
          height="18"
          viewBox="0 0 24 24"
          fill="none"
          style={{ stroke: "var(--fa-dim)" }}
          strokeWidth="1.75"
          strokeLinecap="round"
          strokeLinejoin="round"
        >
          <path d="M14.5 4h-5L7 7H4a2 2 0 0 0-2 2v9a2 2 0 0 0 2 2h16a2 2 0 0 0 2-2V9a2 2 0 0 0-2-2h-3l-2.5-3z" />
          <circle cx="12" cy="13" r="3.5" />
        </svg>
        <span style={{ fontStyle: "italic", color: "var(--fa-dim)" }}>
          and the image pixels
        </span>
      </div>
    </div>
  </div>
  <div
    style={{
      background: "var(--fa-good-bg)",
      border: "1.5px solid var(--fa-good-border)",
      borderRadius: "12px",
      padding: "1.25rem",
      color: "var(--fa-text)",
    }}
  >
    <div
      style={{
        color: "var(--fa-good-text)",
        fontWeight: 700,
        fontSize: "0.75rem",
        textTransform: "uppercase",
        letterSpacing: "0.08em",
        marginBottom: "0.85rem",
      }}
    >
      What lands on my disk
    </div>
    <div
      style={{
        display: "flex",
        flexDirection: "column",
        gap: "0.55rem",
        fontSize: "0.9rem",
        color: "var(--fa-text)",
      }}
    >
      <div
        style={{
          display: "flex",
          alignItems: "center",
          gap: "0.6rem",
          color: "var(--fa-good-text)",
          fontWeight: 600,
        }}
      >
        <svg
          width="18"
          height="18"
          viewBox="0 0 24 24"
          fill="none"
          style={{ stroke: "var(--fa-good-text)" }}
          strokeWidth="2"
          strokeLinecap="round"
          strokeLinejoin="round"
        >
          <polyline points="20 6 9 17 4 12" />
        </svg>
        <span style={{ color: "var(--fa-good-text)" }}>The image pixels</span>
      </div>
      <div
        style={{
          color: "var(--fa-strike)",
          textDecoration: "line-through",
          fontSize: "0.85rem",
        }}
      >
        GPS coordinates
      </div>
      <div
        style={{
          color: "var(--fa-strike)",
          textDecoration: "line-through",
          fontSize: "0.85rem",
        }}
      >
        Phone make and model
      </div>
      <div
        style={{
          color: "var(--fa-strike)",
          textDecoration: "line-through",
          fontSize: "0.85rem",
        }}
      >
        Date and time
      </div>
      <div
        style={{
          color: "var(--fa-strike)",
          textDecoration: "line-through",
          fontSize: "0.85rem",
        }}
      >
        Lens, ISO, exposure
      </div>
    </div>
  </div>
</div>

The cleanup happens during upload, not after. The original file with EXIF intact is never written to disk. The Edge Function reads the upload as it arrives, rewrites it, and saves only the cleaned version to storage.

## Who can see your photos: Row-Level Security (RLS)

Your photos live in **object storage** (files storage). Each file sits under a path that starts with your user ID. A rule called a **Row-Level Security policy** (RLS), enforced by the storage layer itself, lets through only the user whose ID matches that first folder.

<div
  style={{
    background: "var(--fa-bg)",
    border: "1.5px solid var(--fa-border)",
    borderRadius: "12px",
    padding: "1.5rem",
    margin: "1.5rem 0",
    color: "var(--fa-text)",
  }}
>
  <div style={{ display: "flex", flexDirection: "column", gap: "0.85rem" }}>
    <div
      style={{
        display: "flex",
        alignItems: "center",
        gap: "0.85rem",
        flexWrap: "wrap",
      }}
    >
      <div
        style={{
          background: "var(--fa-card-inner-bg)",
          border: "1.5px solid var(--fa-border)",
          borderRadius: "8px",
          padding: "0.6rem 0.85rem",
          fontSize: "0.85rem",
          color: "var(--fa-text)",
        }}
      >
        Alice asks for{" "}
        <strong style={{ color: "var(--fa-text)" }}>alice/photo.jpg</strong>
      </div>
      <svg
        width="22"
        height="22"
        viewBox="0 0 24 24"
        fill="none"
        style={{ stroke: "var(--fa-accent)" }}
        strokeWidth="2"
        strokeLinecap="round"
        strokeLinejoin="round"
      >
        <line x1="5" y1="12" x2="19" y2="12" />
        <polyline points="12 5 19 12 12 19" />
      </svg>
      <div
        style={{
          background: "var(--fa-pill-bg)",
          color: "var(--fa-pill-text)",
          borderRadius: "8px",
          padding: "0.6rem 0.85rem",
          fontWeight: 600,
          fontSize: "0.85rem",
        }}
      >
        RLS: requester = first folder?
      </div>
      <svg
        width="22"
        height="22"
        viewBox="0 0 24 24"
        fill="none"
        style={{ stroke: "var(--fa-good-text)" }}
        strokeWidth="2"
        strokeLinecap="round"
        strokeLinejoin="round"
      >
        <line x1="5" y1="12" x2="19" y2="12" />
        <polyline points="12 5 19 12 12 19" />
      </svg>
      <div
        style={{
          color: "var(--fa-good-text)",
          fontWeight: 700,
          fontSize: "0.9rem",
        }}
      >
        file served
      </div>
    </div>
    <div
      style={{
        display: "flex",
        alignItems: "center",
        gap: "0.85rem",
        flexWrap: "wrap",
      }}
    >
      <div
        style={{
          background: "var(--fa-card-inner-bg)",
          border: "1.5px solid var(--fa-border)",
          borderRadius: "8px",
          padding: "0.6rem 0.85rem",
          fontSize: "0.85rem",
          color: "var(--fa-text)",
        }}
      >
        Bob asks for{" "}
        <strong style={{ color: "var(--fa-text)" }}>alice/photo.jpg</strong>
      </div>
      <svg
        width="22"
        height="22"
        viewBox="0 0 24 24"
        fill="none"
        style={{ stroke: "var(--fa-accent)" }}
        strokeWidth="2"
        strokeLinecap="round"
        strokeLinejoin="round"
      >
        <line x1="5" y1="12" x2="19" y2="12" />
        <polyline points="12 5 19 12 12 19" />
      </svg>
      <div
        style={{
          background: "var(--fa-pill-bg)",
          color: "var(--fa-pill-text)",
          borderRadius: "8px",
          padding: "0.6rem 0.85rem",
          fontWeight: 600,
          fontSize: "0.85rem",
        }}
      >
        RLS: requester = first folder?
      </div>
      <svg
        width="22"
        height="22"
        viewBox="0 0 24 24"
        fill="none"
        style={{ stroke: "var(--fa-bad-text)" }}
        strokeWidth="2"
        strokeLinecap="round"
        strokeLinejoin="round"
      >
        <line x1="5" y1="12" x2="19" y2="12" />
        <polyline points="12 5 19 12 12 19" />
      </svg>
      <div
        style={{
          color: "var(--fa-bad-text)",
          fontWeight: 700,
          fontSize: "0.9rem",
        }}
      >
        403 Forbidden
      </div>
    </div>
  </div>
</div>

The check is enforced by the storage system itself. Every read or write request is filtered through it, no matter where the request comes from.

## Temporary access: signed URLs

To show you a picture, the app hands you a temporary **signed URL**: an access pass that carries three things. The storage refuses the file if any of the three is wrong or out of date.

<div
  style={{
    background: "var(--fa-bg)",
    border: "1.5px solid var(--fa-border)",
    borderRadius: "12px",
    padding: "0",
    margin: "1.5rem 0",
    color: "var(--fa-text)",
    maxWidth: "28rem",
    overflow: "hidden",
  }}
>
  <div
    style={{
      background: "var(--fa-pill-bg)",
      color: "var(--fa-pill-text)",
      padding: "0.65rem 1rem",
      fontWeight: 700,
      fontSize: "0.75rem",
      letterSpacing: "0.08em",
      textTransform: "uppercase",
    }}
  >
    Temporary access pass
  </div>
  <div
    style={{
      display: "flex",
      alignItems: "center",
      gap: "0.85rem",
      padding: "0.9rem 1rem",
      borderBottom: "1px dashed var(--fa-dashed)",
      color: "var(--fa-text)",
    }}
  >
    <svg
      width="28"
      height="28"
      viewBox="0 0 24 24"
      fill="none"
      style={{ stroke: "var(--fa-accent)" }}
      strokeWidth="1.5"
      strokeLinecap="round"
      strokeLinejoin="round"
    >
      <path d="M22 19a2 2 0 0 1-2 2H4a2 2 0 0 1-2-2V5a2 2 0 0 1 2-2h5l2 3h9a2 2 0 0 1 2 2z" />
    </svg>
    <div style={{ flex: 1, color: "var(--fa-text)" }}>
      <div
        style={{
          fontSize: "0.7rem",
          fontWeight: 700,
          color: "var(--fa-dim)",
          textTransform: "uppercase",
          letterSpacing: "0.06em",
        }}
      >
        File
      </div>
      <div
        style={{
          fontSize: "0.95rem",
          fontWeight: 600,
          color: "var(--fa-text)",
        }}
      >
        which photo this pass unlocks
      </div>
    </div>
  </div>
  <div
    style={{
      display: "flex",
      alignItems: "center",
      gap: "0.85rem",
      padding: "0.9rem 1rem",
      borderBottom: "1px dashed var(--fa-dashed)",
      color: "var(--fa-text)",
    }}
  >
    <svg
      width="28"
      height="28"
      viewBox="0 0 24 24"
      fill="none"
      style={{ stroke: "var(--fa-info)" }}
      strokeWidth="1.5"
      strokeLinecap="round"
      strokeLinejoin="round"
    >
      <rect x="3" y="11" width="18" height="11" rx="2" ry="2" />
      <path d="M7 11V7a5 5 0 0 1 10 0v4" />
    </svg>
    <div style={{ flex: 1, color: "var(--fa-text)" }}>
      <div
        style={{
          fontSize: "0.7rem",
          fontWeight: 700,
          color: "var(--fa-dim)",
          textTransform: "uppercase",
          letterSpacing: "0.06em",
        }}
      >
        Stamp
      </div>
      <div
        style={{
          fontSize: "0.95rem",
          fontWeight: 600,
          color: "var(--fa-text)",
        }}
      >
        cryptographic proof I authorized it
      </div>
    </div>
  </div>
  <div
    style={{
      display: "flex",
      alignItems: "center",
      gap: "0.85rem",
      padding: "0.9rem 1rem",
      color: "var(--fa-text)",
    }}
  >
    <svg
      width="28"
      height="28"
      viewBox="0 0 24 24"
      fill="none"
      style={{ stroke: "var(--fa-good-text)" }}
      strokeWidth="1.5"
      strokeLinecap="round"
      strokeLinejoin="round"
    >
      <circle cx="12" cy="12" r="10" />
      <polyline points="12 6 12 12 16 14" />
    </svg>
    <div style={{ flex: 1, color: "var(--fa-text)" }}>
      <div
        style={{
          fontSize: "0.7rem",
          fontWeight: 700,
          color: "var(--fa-dim)",
          textTransform: "uppercase",
          letterSpacing: "0.06em",
        }}
      >
        Expires
      </div>
      <div
        style={{
          fontSize: "0.95rem",
          fontWeight: 600,
          color: "var(--fa-text)",
        }}
      >
        15 minutes from when it was issued
      </div>
    </div>
  </div>
</div>

A fresh pass is generated each time an actually authenticated user on the app needs to display a photo. Once a pass expires it cannot be renewed; the app simply requests a new one with a new deadline.

## Third-party products in the try-on chain

[MirrAI](https://mirraitryon.com) is not the only product in the chain. Two others sit between your phone and the final result.

<div
  style={{
    display: "flex",
    flexDirection: "column",
    gap: "0.65rem",
    margin: "1.5rem 0",
  }}
>
  <div
    style={{
      background: "var(--fa-bg)",
      border: "1.5px solid var(--fa-border)",
      borderRadius: "12px",
      padding: "1rem 1.25rem",
      display: "flex",
      alignItems: "center",
      gap: "1rem",
      color: "var(--fa-text)",
    }}
  >
    <svg
      width="32"
      height="32"
      viewBox="0 0 24 24"
      fill="none"
      style={{ stroke: "var(--fa-text)" }}
      strokeWidth="1.5"
      strokeLinecap="round"
      strokeLinejoin="round"
    >
      <rect x="5" y="2" width="14" height="20" rx="2" />
      <line x1="12" y1="18" x2="12.01" y2="18" />
    </svg>
    <div style={{ flex: 1, color: "var(--fa-text)" }}>
      <div
        style={{
          fontWeight: 700,
          fontSize: "0.95rem",
          color: "var(--fa-text)",
        }}
      >
        Your device
      </div>
      <div
        style={{
          fontSize: "0.85rem",
          color: "var(--fa-dim)",
          marginTop: "0.15rem",
        }}
      >
        where the photo is born, and where the result is shown back
      </div>
    </div>
  </div>
  <div
    style={{
      textAlign: "center",
      color: "var(--fa-accent)",
      fontSize: "0.75rem",
      fontWeight: 700,
      letterSpacing: "0.06em",
    }}
  >
    ↓ uploaded over HTTPS (encrypted in transit)
  </div>
  <div
    style={{
      background: "var(--fa-bg)",
      border: "1.5px solid var(--fa-border)",
      borderRadius: "12px",
      padding: "1rem 1.25rem",
      display: "flex",
      alignItems: "center",
      gap: "1rem",
      color: "var(--fa-text)",
    }}
  >
    <svg
      width="32"
      height="32"
      viewBox="0 0 24 24"
      fill="none"
      style={{ stroke: "var(--fa-text)" }}
      strokeWidth="1.5"
      strokeLinecap="round"
      strokeLinejoin="round"
    >
      <circle cx="12" cy="12" r="3" />
      <path d="M19.4 15a1.65 1.65 0 0 0 .33 1.82l.06.06a2 2 0 1 1-2.83 2.83l-.06-.06a1.65 1.65 0 0 0-1.82-.33 1.65 1.65 0 0 0-1 1.51V21a2 2 0 1 1-4 0v-.09a1.65 1.65 0 0 0-1-1.51 1.65 1.65 0 0 0-1.82.33l-.06.06a2 2 0 1 1-2.83-2.83l.06-.06a1.65 1.65 0 0 0 .33-1.82 1.65 1.65 0 0 0-1.51-1H3a2 2 0 1 1 0-4h.09a1.65 1.65 0 0 0 1.51-1 1.65 1.65 0 0 0-.33-1.82l-.06-.06a2 2 0 1 1 2.83-2.83l.06.06a1.65 1.65 0 0 0 1.82.33h0a1.65 1.65 0 0 0 1-1.51V3a2 2 0 1 1 4 0v.09a1.65 1.65 0 0 0 1 1.51h0a1.65 1.65 0 0 0 1.82-.33l.06-.06a2 2 0 1 1 2.83 2.83l-.06.06a1.65 1.65 0 0 0-.33 1.82v0a1.65 1.65 0 0 0 1.51 1H21a2 2 0 1 1 0 4h-.09a1.65 1.65 0 0 0-1.51 1z" />
    </svg>
    <div style={{ flex: 1, color: "var(--fa-text)" }}>
      <div
        style={{
          fontWeight: 700,
          fontSize: "0.95rem",
          color: "var(--fa-text)",
        }}
      >
        <a href="https://mirraitryon.com">MirrAI</a> server (Edge Function)
      </div>
      <div
        style={{
          fontSize: "0.85rem",
          color: "var(--fa-dim)",
          marginTop: "0.15rem",
        }}
      >
        run by me. Strips EXIF, checks who you are, generates signed URLs.
      </div>
    </div>
  </div>
  <div
    style={{
      textAlign: "center",
      color: "var(--fa-accent)",
      fontSize: "0.75rem",
      fontWeight: 700,
      letterSpacing: "0.06em",
    }}
  >
    ↓ sends the photo for try-on inference
  </div>
  <div
    style={{
      background: "var(--fa-bg)",
      border: "1.5px solid var(--fa-border)",
      borderRadius: "12px",
      padding: "1rem 1.25rem",
      display: "flex",
      alignItems: "center",
      gap: "1rem",
      color: "var(--fa-text)",
    }}
  >
    <svg
      width="32"
      height="32"
      viewBox="0 0 24 24"
      fill="none"
      style={{ stroke: "var(--fa-text)" }}
      strokeWidth="1.5"
      strokeLinecap="round"
      strokeLinejoin="round"
    >
      <rect x="3" y="3" width="18" height="18" rx="2" />
      <path d="M9 9h6v6H9z" />
      <path d="M9 1v6M15 1v6M9 17v6M15 17v6M1 9h6M17 9h6M1 15h6M17 15h6" />
    </svg>
    <div style={{ flex: 1, color: "var(--fa-text)" }}>
      <div
        style={{
          fontWeight: 700,
          fontSize: "0.95rem",
          color: "var(--fa-text)",
        }}
      >
        Vertex AI (Google Cloud)
      </div>
      <div
        style={{
          fontSize: "0.85rem",
          color: "var(--fa-dim)",
          marginTop: "0.15rem",
        }}
      >
        run by Google. The actual try-on model. Receives the photo, returns a
        result, does not retain it.
      </div>
    </div>
  </div>
  <div
    style={{
      textAlign: "center",
      color: "var(--fa-accent)",
      fontSize: "0.75rem",
      fontWeight: 700,
      letterSpacing: "0.06em",
    }}
  >
    ↓ photo and result stored
  </div>
  <div
    style={{
      background: "var(--fa-bg)",
      border: "1.5px solid var(--fa-border)",
      borderRadius: "12px",
      padding: "1rem 1.25rem",
      display: "flex",
      alignItems: "center",
      gap: "1rem",
      color: "var(--fa-text)",
    }}
  >
    <svg
      width="32"
      height="32"
      viewBox="0 0 24 24"
      fill="none"
      style={{ stroke: "var(--fa-text)" }}
      strokeWidth="1.5"
      strokeLinecap="round"
      strokeLinejoin="round"
    >
      <ellipse cx="12" cy="5" rx="9" ry="3" />
      <path d="M21 12c0 1.66-4 3-9 3s-9-1.34-9-3" />
      <path d="M3 5v14c0 1.66 4 3 9 3s9-1.34 9-3V5" />
    </svg>
    <div style={{ flex: 1, color: "var(--fa-text)" }}>
      <div
        style={{
          fontWeight: 700,
          fontSize: "0.95rem",
          color: "var(--fa-text)",
        }}
      >
        Supabase Storage
      </div>
      <div
        style={{
          fontSize: "0.85rem",
          color: "var(--fa-dim)",
          marginTop: "0.15rem",
        }}
      >
        run by Supabase. Hosts the files, encrypted at rest, locked to your
        account by the RLS rule above.
      </div>
    </div>
  </div>
</div>

Vertex AI only sees the photo it needs to process, and nothing else. [Supabase](https://supabase.com/) is the data and files storage service.

## The whole pipeline

From upload to result, your photo passes through three services. The [MirrAI](https://mirraitryon.com) **Edge Function** strips its EXIF metadata. **Vertex AI** (Google) runs the try-on. **Supabase Storage** holds both the original and the result under an **RLS** policy locked to your account folder. Whenever the app shows you a picture, it generates a **signed URL** that lasts fifteen minutes.
